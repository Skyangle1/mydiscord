const { EmbedBuilder, ActionRowBuilder, ButtonBuilder, ButtonStyle } = require('discord.js');

// The updateBondedHousePanel function has been disabled to prevent disturbance
// The bonded house panel is only sent once when the /bonded-house command is used

module.exports = {};